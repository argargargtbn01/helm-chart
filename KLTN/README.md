# KLTN Service Helm Chart

## Giới thiệu
Một Helm chart linh hoạt để triển khai tất cả các service trong dự án KLTN.

## Cài đặt
```bash
helm install <release-name> ./KLTN --values <path-to-values.yaml>